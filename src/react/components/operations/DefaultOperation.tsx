import { Box } from "@mui/material";
import React from "react";

/**
 * A React functional component that renders the "DefaultOperation" operation UI
 *
 * @returns {*} The rendered "DefaultOperation" component
 */
const DefaultOperation: React.FC = () => {
    return <Box>Invalid input operation!</Box>;
}

export default DefaultOperation;