import { Box } from "@mui/material";
import React from "react";

/**
 * A React functional component that renders the "ShowProperties" operation UI
 *
 * @returns {*} The rendered "ShowProperties" component
 */
const ShowProperties: React.FC = () => {
  return <Box>Showing properties...</Box>;
};

export default ShowProperties;
